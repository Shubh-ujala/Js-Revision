"use strict"; //treat all the JS code as a newer version
//we will go thorugh two documentations (i)mdn & (ii) tc39.es

//alert("hello")  //it will through an error because we are using nodeJs not a browser 

console.log(3
    +
    3); //bad practice code must be readable

    let name = "Shubh"
    let age = 18
    let isLoggedIn = false
    let state;
    
    /* 
    DATATYPES IN JS
      number => 2^53
      bigint//big integer
      string => ""
      boolean => true/false
      null => standalone value
      undefined 
      symbol => unique
      object
    */
     
    console.log(typeof undefined); // output-> undefined
    console.log(typeof null); // output-> object